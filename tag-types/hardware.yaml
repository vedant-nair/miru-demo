title: Hardware Metadata
type: object
properties:
  payload_capacity:
    type: number
  default_speed_mode:
    type: string
    enum: [slow, normal, fast]
required:
  - payload_capacity
  - default_speed_mode