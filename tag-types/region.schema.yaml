title: region
type: object
properties:
  telemetry_upload_interval_sec:
    type: integer
    minimum: 10
    maximum: 300
  heartbeat_interval_sec:
    type: integer
    minimum: 1
    maximum: 60
required:
  - telemetry_upload_interval_sec
  - heartbeat_interval_sec
